import 'package:flutter/material.dart';

class AppColors {
  // static const Color background = Color(0xFF001F3F); // darkBlue
  // static const Color secondary = Color(0xFFfca311); // Gold-ish
  // static const Color background = Color.fromARGB(255, 31, 30, 30);
  // static const Color secondary = Color.fromARGB(255, 233, 232, 230);
  // static const Color background = Color.fromARGB(255, 31, 30, 30);
  static const Color background = Color(0xFF0B335F);
  static const Color secondary = Color.fromARGB(255, 233, 232, 230);
  static const Color cardBackground = Color(0xFFe9ecef); // Light Gold
  static const Color text = Color(0xFF001F3F);
  static const Color warmBeige = Color(0xFFD8CBB7); // Warm Beige
  static const Color filterPanel = Colors.white;
  static const Color settings = Colors.white;
  static const Color archived = Colors.white;
  static const Color completedReports = Colors.white;
  static const Color createReport = Colors.white;
  static const Color map = Colors.white;
  static const Color dashboard = Colors.white;
}
